/*You are given an array of n integers and a target value. Write a program to check if the target exists in the array.
If it exists, print the 0-based index of its first occurrence. If it does not exist, print -1.*/
import java.io.*;
import java.util.*;

public class Solution {

    public static void main(String[] args) {
        /* Enter your code here. Read input from STDIN. Print output to STDOUT. Your class should be named Solution. */
        Scanner sc=new Scanner(System.in);
        int n=sc.nextInt();
        int arr[]=new int[n];
        
        for( int i=0;i<n;i++){
            arr[i]=sc.nextInt();
        }
        
        int f=sc.nextInt();
        int index=-1;
        for(int i=0;i<n;i++){
         if(arr[i]==f){
            
            index=i;
             break;
         }
        }
        
            System.out.println(index);
}
}
